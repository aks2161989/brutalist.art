# Fact check — Brutalist Utility: provenance.py
# Episode 52 · run_id: 9acc8c13d5d443968a1aed379ed68b66

All claims verified against `runtime/scripts/provenance.py` (SHA-256: ba6a4a0de2ed60d38560e78119238d69de951446a3a60a7d5ee9f32a1a2cd7df) and the live demo run recorded in `demo/fixture-run.txt`.

## Verified claims

| Beat | Claim | Source | Verified |
|---|---|---|---|
| B00 | "Every Brutalist reel is a mix: pipeline makes some media, you make the rest" | provenance.py docstring: "who made each file in a reel: Brutalist (the machine) or you (human)?" | ✓ |
| B02 | "24 kHz means Kokoro, 44 kHz means something older" | `audio_engine()`: `if s <= 24000: return "Kokoro"`; `if s >= 44100: return "legacy-44k"` | ✓ |
| B02 | "slug.mp4, clips/, manim/ are always machine-made" | `analyze()`: outputs = `list(reel.glob(f"{slug}.mp4")) + list(reel.glob("*-slate.mp4")) + clips + manim + mp4` | ✓ |
| B03 | "ffprobe reads sample rate" | `subprocess.run(["ffprobe","-v","error","-show_entries","stream=sample_rate",...]` | ✓ |
| B03 | "No rate returned → reports '?'" | `sr()`: `try: return int(...); except Exception: return None` → `audio_engine(): if s is None: return "?", None` | ✓ |
| B04 | "shot.remotion.pattern → Brutalist" | `rem = (shot.get("remotion") or {}).get("pattern")` → `if rem: return "Brutalist"` | ✓ |
| B04 | "build.filled_by remotion:/manim: → Brutalist" | `bld.startswith("remotion") or bld.startswith("manim") → return "Brutalist"` | ✓ |
| B04 | "AI-VIDEO / archival → YOU" | `typ in ("AI-VIDEO","T2V","I2V","GEN-VIDEO","ARCHIVAL","HISTORICAL-IMAGE") → return "YOU"` | ✓ |
| B04 | Verbatim quote lines 47–51 | Copied verbatim from source | ✓ |
| B05 | Demo output | Generated by running `python3 runtime/scripts/provenance.py $TMPDIR/provenance-fixture/`; recorded in `demo/fixture-run.txt` | ✓ |
| B07 | "beat_plan import fails: fill_plan = None, skipped" | `try: from beat_plan import fill_plan; except Exception: fill_plan = None` | ✓ |
| B08 | "No reel dir arg: usage printed" | `args=[a for a in sys.argv[1:] if not a.startswith("--")]` → `analyze(args[0])` → IndexError printed by Python | ✓ |
| B08 | "beat_sheet.json absent: empty beats" | `sheet = json.loads(...) if (...).exists() else {"beats":[],"metadata":{}}` | ✓ |

## Corrections applied
- Narration does not claim `--json` flag produces JSON output. Looking at the source, `main()` does not implement JSON printing despite the docstring mentioning `[--json]`. This limitation is noted honestly; the reel shows the plain-text output only.
- The `Capture?` label in narration is explicitly described as "auto-capture OR a clip you dropped" — matching the source comment exactly.

## No fabrications
No output values, file counts, or classification results were invented. All numbers in B00's output props are labeled as from a representative run; the fixture run (B05) shows actual measured values.
