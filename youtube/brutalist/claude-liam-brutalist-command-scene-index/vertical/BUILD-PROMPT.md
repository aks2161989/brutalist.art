Portrait companion — same authorship contract as the landscape
[`../BUILD-PROMPT.md`](../BUILD-PROMPT.md). No aliases branch: the
dispatcher case at art:71-74 matches only the exact string
`scene-index`. Portrait re-uses that identical dispatcher; only the
Remotion compositions change to their `*916` variants.
