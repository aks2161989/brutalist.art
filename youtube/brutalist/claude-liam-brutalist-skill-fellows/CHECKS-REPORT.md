# Checks report — landscape

Reel: `The fellows Skill` — `claude-liam-brutalist-skill-fellows`
Aspect: 16:9 · 3840×2160 · 24 fps · 261.88 s · aac stereo
Run: `616bfc2fadd2479fadc72138c055fa1a` (re-verified; previous run `e71dd104` — SKILL.md content unchanged, toolkit baseline updated)

## Beat classification (PROOF GATE from `nopunt` SKILL.md)

12 SHOW / 0 justified-HOLD / 0 PUNT-flagged.
Every body beat classifies SHOW: it names its on-screen artifact in `shot.show`
and the visual carries the evidence the voice reacts to. Bookend beats (B00 ASK,
B11 OUTRO) render UI directly. No unresolved slates.

## Teaching arc checklist (nopunt § whole-sheet)

- FRAMEWORK — ✓ (B02 anatomy + B03 pipeline, before any mechanism act)
- WORKED EXAMPLE — ✓ (B04 THE REPORT IS THE CLOCK with the concrete 4-never-do
  list and verbatim quote; B06 GATE N with the exact `notes_sha256`/`narration_sha256`
  fingerprint pair)
- FALSIFIABILITY — ✓ (B08 SIBLING BREAK explicitly names the wrong-outro defect;
  verdict pill uses negative polarity)
- SCAFFOLDED TASK — ✓ (B10 Your Turn — Liam reads the paste-ready dry-run prompt
  aloud and discusses the 3-check rubric before inviting the pause)
- BOOKENDS — ✓ (B00 cold open · B01 hesitant-writer BLUF · B09 verdict card ·
  B10 handoff · B11 title-restate outro)
- NO-SOURCE-NO-VERDICT — ✓ (every substantive claim in narration is traced to a
  SKILL.md line in `FACTCHECK.md`; no fabricated fellow's report)

## Motion histogram

`reveal:8  type-on:3  hold:1` — compile.py warns that `reveal` carries 66% of
beats vs. the ~40% pantry cap. Accepted here: the skill-teardown modifier is
structurally reveal-heavy (each mechanism/falsifiability beat reveals eyebrow →
heading → body → verbatim quote → verdict pill). The two type-on composer beats
(B00, B10), the hesitant-writer type-on (B01) and the hold outro (B11) do the
pacing work. This warning is not a Gate refusal — the final master compiled
cleanly through the atomic verification.

## Kokoro audio (master clock)

All 12 beats generated by `runtime/scripts/generate_audio_kokoro.py` using
voice `am_onyx` (free, local). Actual durations (measured from mp3 decode):

| Beat | Actual (s) | Kind |
|---|---|---|
| B00 | 13.16 | ASK cold open |
| B01 | 14.38 | BLUF hesitant writer |
| B02 | 21.65 | FRAMEWORK · anatomy |
| B03 | 19.78 | FRAMEWORK · pipeline |
| B04 | 23.72 | MECH · REPORT IS THE CLOCK |
| B05 | 22.55 | MECH · FELLOW'S-WORK CARVE-OUT |
| B06 | 27.43 | MECH · GATE N |
| B07 | 26.84 | MECH · PERSISTENT VOICE + SOUND REPAIR |
| B08 | 26.26 | FALSIFIABILITY · SIBLING BREAK |
| B09 | 26.73 | VERDICT · artifact |
| B10 | 35.73 | YOUR TURN · handoff |
| B11 | 3.37  | OUTRO · title restate |
| **Total** | **261.60** | |

Compiled file duration: 261.88 s (per-beat frame-alignment padding accounts
for the ~0.28 s delta).

## Compile

Command:
```
python3 runtime/scripts/compile.py <reel> --height 2160 --force --out <reel>/exports/landscape
```

Result:
```
[art] compiled B00  VIDEO    13.2s  ← B00.mp4
… (12 beats compiled, all VIDEO)
[art] motion histogram: reveal:8  type-on:3  hold:1
[art] wrote …/claude-liam-brutalist-skill-fellows.mp4  (261.9s, audio: per-beat timeline …)
[art] slots: 12/12 filled — B00:VIDEO … B11:VIDEO
```

ffprobe on the final MP4:
```
$ ffprobe -v error -select_streams v:0 -show_entries stream=width,height,r_frame_rate,duration -of csv=p=0
3840,2160,24/1,261.875000
$ ffprobe -v error -select_streams a:0 -show_entries stream=codec_name,channels -of csv=p=0
aac,2
```

## Gate V (frame-level visual QC)

Ran as part of `compile.py` and passed cleanly on the second attempt (`frames=24
BLOCKER=0 MAJOR=0`). The first attempt reported one MAJOR `underfill` on
`B01_50.png` (hesitant writer typing pace at 50% of beat left the bottom half
empty). Repaired by reducing `charMs` 28→20 and `hesitateBetween` 8→3 so all
four lines finish by the 50% steady-state sample. Root-cause fix in the beat
sheet, not in the check.

`_qc/REPORT.md` records the final clean pass. `.verified.json` receipt sits
next to the export in `exports/landscape/` (SHA-256 4b4b6d00…).

## Visual review (LLM-eye pass)

Extracted 12 hero frames (one per beat, at the beat mid-point of the landscape
master), plus the Gate V contact sheet, and inspected them directly with the
image-capable Read tool. Frames live at `_qc/frames/L_*.jpg`; combined contact
sheet at `_qc/contact-landscape.png`. Findings:

- B00 — composer card, "Hallo, Liam" serif greeting, ask command fully typed,
  terracotta send button armed, three result lines below, `@HumanitariansAI`
  folder chip visible. No overflow.
- B01 — hesitant writer finishes at "The fellows skill frames the fellows
  video. The fellows report plays untouched." `re-cuts → frames` correction is
  in the completed timeline. No overflow.
- B02 — anatomy card is legible; folder tree resolves; callout "NOTES.md is a
  human gate." is legible; spark line "A frame around a fellow's file."
- B03 — pipeline card shows INPUT → ASK → SUMMARY ×2–3 → THE REPORT (terracotta) →
  BEAR'S NOTES ×2 → YOUR TURN + HAI OUTRO → OUTPUT. Footer note lands.
- B04–B08 — mechanism/falsifiability cards each carry eyebrow, heading, body,
  verbatim SKILL.md quote (with citation), and verdict pill. B08's verdict pill
  is negative-polarity (`verdictPositive: false`) — "wrong outro = defect."
- B09 — verdict artifact card lists six numbered lines; heading "A frame. Not a
  re-edit." lands.
- B10 — Your Turn composer types the paste-ready dry-run prompt; three-line
  rubric appears as output.
- B11 — outro shows "The fellows Skill." with terracotta period, hardcoded
  `@NikBearBrown` handle, slug-seeded mascot.

No overlap, no clipping past safe, no unwanted captions/subtitles, no
unresolved slates. Every beat teaches the concept the narration describes.

## Availability log

- `runtime/qc/final_frame_check.py` (Gate V) IS present and PASSED
  (0 BLOCKER, 0 MAJOR, 24 frames).
- `scripts/type_check.py` (legacy "GATE T · ALWAYS RUN" from ai-explainer §Hard
  rules) is NOT present in this toolkit; the substitute visual typography
  review is in `TYPECHECK.md`. Not reported as a pass.
- `scenes/ILLUSTRATIONS.md` is absent; the C3 starter templates named in
  ai-explainer (LayerStack, SourceFlow, ChipGrid, PredictCard) are searchable
  as `Illu-*` scenes via `./art scenes`; none were needed for this teardown.

## Conclusion

Landscape master: **CLEAN**. All checks that exist in this toolkit passed.
Bear's viewing/editing is next; the file is not published.
