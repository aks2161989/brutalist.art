# SHOTLIST — Download Brutalist with Claude Code

One row per beat. All shots on the CLAUDE fidelity palette: cream `#FAF9F5` page, warm ink `#3D3929`, terracotta `#D97757` spark (the ONE accent).

| Beat | Composition | Aspect (LS / P) | On-screen | Duration |
|------|-------------|-----------------|-----------|----------|
| B00 | `ClaudeComposerAsk` / `ClaudeComposerAsk916` | HUMANITARIANS AI · BRUTALIST eyebrow · "Download Brutalist" segment · `* Ciao, Liam` greeting · composer types the ask · `* reading the setup script…` running indicator · three output lines below (`git clone …`, `./setup --install …`, `./setup …`) · @HumanitariansAI folder chip. | 12.99s |
| B01 | `BrutalistHesitantWriter` / `BrutalistHesitantWriter916` | Serif type-on: writer types `Downloading Brutalist means running an installer.`, pauses, marks `running an installer` in terracotta, deletes it, types `a git clone plus a free doctor script`. Seed `dbc-b01` (deterministic). | 18.67s (lead 0.8s) |
| B02 | `DbcPasteReady` / `DbcPasteReady916` | BRUTALIST · PASTE-READY eyebrow · title "One paste. Two commands." (spark on the second sentence) · composer-shaped card holds the ask (`Clone https://github.com/nikbearbrown/brutalist.art …`) with `press ↵ to send` and the terracotta send button · three chips beneath: **01 · CLONE** `git clone → your folder`, **02 · INSTALL** `./setup --install (free deps + Kokoro)`, **03 · VERIFY** `./setup → readiness table`. | 16.94s |
| B03 | `DbcCloneTree` / `DbcCloneTree916` | BRUTALIST · WHAT LANDS ON DISK eyebrow · title "A folder. Nothing outside it." · single card with `brutalist.art/` header and seven children arriving in order: `setup` (▪, ENTRY chip terracotta), `art` (▪, ENTRY chip terracotta), `README.md` (·), `skills/` (▸), `runtime/` (▸), `docs/` (▸), `brands/` (▸) with plain-english notes on the right. Sparkline "A clone is a folder. Everything lives inside it." bottom. | 19.18s |
| B04 | `DbcDoctor` / `DbcDoctor916` | BRUTALIST · READINESS TABLE eyebrow · title "Green means ready." · dark terminal card: `$ ./setup`, `brutalist — readiness (free-only toolkit: no keys, ever)`, FEATURE / DEPENDENCIES / STATUS header, seven feature rows arriving in sequence — all `✓ ready` in green — closing green line `All core features ready. Voices: Bella (af_bella) and Onyx (am_onyx). Cost: $0.00.` | 20.57s |
| B05 | `DbcLanes` / `DbcLanes916` | BRUTALIST · THREE LANES eyebrow · title "Subscription. Optional key. Free pipeline." (spark on third sentence) · three lane cards: **01 · YOUR SUBSCRIPTION — Claude Code** (terracotta accent), **02 · PAID · OPTIONAL — Anthropic API** (ghost, muted), **03 · FREE · LOCAL — Brutalist pipeline** (ink) · terracotta banner underneath: `SETUP COST $0.00 — Brutalist itself never asks for a card.` | 21.55s |
| B06 | `DbcNotClaims` / `DbcNotClaims916` | BRUTALIST · SETUP HONESTY eyebrow · title "What setup won't do." · three rows: **No accounts created.** — `setup asks for zero credentials.`; **No files outside this folder.** — `everything lives inside your clone.`; **No upload. One download.** — `~340MB Kokoro voice model from a GitHub release, the first --install.` Sparkline "The tool has a shape. That shape is the honesty." bottom. | 19.20s |
| B07 | `DbcVerdict` / `DbcVerdict916` | Verdict artifact card fills the safe area · title bar `● Verdict … BRUTALIST · DOWNLOAD` · heading "Download Brutalist, in three lines." · three terracotta-numbered lines · italic sparkline "That is how you download Brutalist." at bottom. | 11.48s |
| B08 | `ClaudeComposerAsk` / `ClaudeComposerAsk916` | HUMANITARIANS AI · BRUTALIST eyebrow · "Download Brutalist" segment · `Your Turn` greeting · composer types the paste-ready prompt (`In this empty folder, clone https://github.com/nikbearbrown/brutalist.art …`) · `* paste this into Claude…` running indicator · three expected-outcome lines. | 18.39s |
| B09 | `DbcOutro` / `DbcOutro916` | Poster serif title "Download Brutalist with Claude Code." (terracotta period), BRUTALIST · EPISODE 02 eyebrow with terracotta rule underneath, **@HumanitariansAI** handle mid-lower (HARDCODED — bypasses `ClaudeTitleOutro`'s `@NikBearBrown` lock), "LIAM, IN FOR BEAR." subline at bottom. | 5.91s |

**Total run time:** ≈ 165.1s (2:45).

**Bookend contract satisfied:** B00 is `ClaudeComposerAsk` (COLD OPEN LAW), B01 is `BrutalistHesitantWriter` with ≥9s audio and `lead_silence_s: 0.8` (EXECUTIVE SUMMARY LAW), B08 is `ClaudeComposerAsk` with `greeting: "Your Turn"` (HANDOFF LAW), B09 restates the title with the HAI handle (OUTRO LAW, adapted for @HumanitariansAI via `DbcOutro`).

**ILLUSTRATE LAW satisfied:** Only two beats use the Claude UI (B00 ask, B08 handoff). The other seven body beats are concept illustrations (`DbcPasteReady`, `DbcCloneTree`, `DbcDoctor`, `DbcLanes`, `DbcNotClaims`, `DbcVerdict`) or the hesitant writer (`BrutalistHesitantWriter`).
