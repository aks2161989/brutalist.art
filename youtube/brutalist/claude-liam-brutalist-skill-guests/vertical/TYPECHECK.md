# TYPECHECK — portrait 9:16

Same gap as landscape: legacy `scripts/type_check.py` and
`reference/type-spec.md` are NOT present in this isolated toolkit
(`ls scripts/` and `ls reference/` confirm). Substitute visual typography
review performed by opening each hero JPEG with the image-capable Read tool.

## Portrait-specific findings

- **B00** — greeting `Hallo, Liam` reflows above a portrait-native composer;
  ask spans multiple lines but no mid-word truncation; folder chip
  `@HumanitariansAI` inside the composer chrome.
- **B01** — five-line serif at fontSize 130 / lineSpacing 2.4. Correction
  `copies → prunes` visible; final sentence fills the portrait safe area at
  ≈57 % (>= FILL_MIN 0.55). No mid-word truncation of the wrap of `copies
  the / fellows spine.`. Serif and warm-ink colours match landscape.
- **B02** — folder tree reflows vertically; every row legible; callout box
  visible below the tree with `REVIEW.md` bolded.
- **B03** — pipeline reflows vertically INPUT ↓ COLD OPEN ↓ SUMMARY ↓ THE
  VIDEO ↓ RECAP ↓ YOUR TURN + HAI OUTRO ↓ OUTPUT; THE VIDEO accented
  terracotta as in landscape.
- **B04–B08** — mechanism cards keep eyebrow / heading / body / verbatim
  quote / verdict pill within portrait safe; the quote block wraps
  gracefully; B08 negative-polarity pill remains visible.
- **B09** — verdict artifact card visible on the contact sheet; six numbered
  lines within safe.
- **B10** — Your Turn composer stacks. Paste-ready prompt reflows over
  multiple sans-serif lines; the three-line rubric under the folder chip is
  legible in mono.
- **B11** — outro title `The guests Skill.` + `@NikBearBrown` handle
  centred; portrait outro chrome adds top/bottom double-rule bars.
- Palette-fidelity: cream / warm-ink / terracotta ratios match landscape;
  each beat carries one accent moment (no double-accent).

No typography defect blocks the portrait pass. Every runnable check ran and
passed. Human review pending.
