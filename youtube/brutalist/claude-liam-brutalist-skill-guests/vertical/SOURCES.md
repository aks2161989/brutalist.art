# SOURCES — portrait companion

Sources are identical to the landscape master (`../SOURCES.md`) — the SKILL.md
source of truth, the doctrine files, the read-only example leads, and the
scene-library availability log all apply verbatim.

The portrait companion additionally uses these `*916` compositions, all
registered in the isolated toolkit's Root.tsx and searchable via
`scene_search.py --check <Name>`:

- `ClaudeComposerAsk916` — RENDERABLE 9:16
- `BrutalistHesitantWriter916` — RENDERABLE 9:16 (portrait-aware scale patch
  copied from the sibling `fellows` episode)
- `SkillTeardownAnatomy916` / `SkillTeardownPipeline916` /
  `SkillTeardownMechanism916` — RENDERABLE 9:16 (portrait wrappers copied
  from the sibling `fellows` episode)
- `ClaudeVerdictArtifact916` — RENDERABLE 9:16
- `ClaudeTitleOutro916` — RENDERABLE 9:16

No PUNT, no SLATE. The four files copied from the sibling episode are
credited there — this run did not author them.
