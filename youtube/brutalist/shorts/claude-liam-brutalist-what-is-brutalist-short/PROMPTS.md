# PROMPTS.md — What Is Brutalist? — Short

*Every "ask" the Short actually shows on screen. The parent B08 fellows CTA is
dropped in this cut, so the paste-ready clone/setup prompt does not appear in
this file.*

## B00 — the cold-open ask (only composer in the Short)

**Composer topic:** `HUMANITARIANS AI · BRUTALIST`
**Segment:** `What Is Brutalist?`
**Greeting:** `Ciao, Liam`
**Running text:** `reading the toolkit…`
**Folder chip:** `@HumanitariansAI`
**Model / effort:** `Claude` / `Desktop`

**Ask (typed into the composer):**

```
Explain what Brutalist actually is. Not the slogan — the thing on disk.
```

**Result lines (shown beneath the composer):**

- a beat sheet is a plain-text recipe
- the film is a pure function of the recipe
- judgment stays human — everything else automates

## B04 — the one edited property shown in WibChange

The Short's single concrete demonstration. Both the JSON edit and the shell
command are rendered inside `WibChange916`; no external shell runs.

```
"greeting": "Ciao, Liam"       →       "greeting": "Hola, Liam"
```

Shell line rendered under the code:

```
$ python3 remotion_scenes.py --only B00
→ re-rendering B00.mp4…
```

`remotion_scenes.py --only <BID>` is the actual documented flag (see
`python3 runtime/scripts/remotion_scenes.py --help`). No fictional command.
The composer preview below the JSON updates from `Ciao, Liam` /
`Italian "hello."` to `Hola, Liam` / `Spanish "hello."` — the exact swap the
narration describes.

## What the Short deliberately does NOT show

- **The paste-ready fellows CTA (parent B08).** Dropped in this cut. Fellows
  who want the clone/`./setup --install` prompt should watch the 16:9 long
  or the parent 9:16 companion.
- **No public URL is spoken or displayed** in the retained beats. The Short
  is a concept explainer, not a distribution vehicle.

## Where a paid gate would apply (labeled, never faked)

None on the fellow path. `Optional Higgsfield` (`CLAUDE.md §OPTIONAL UPGRADE`)
would gate certain AI-video beats — but Brutalist's default is Ken Burns
stills. This Short makes no Higgsfield ask.
