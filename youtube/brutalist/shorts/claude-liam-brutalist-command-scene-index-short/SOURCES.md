# SOURCES — Short (9:16) — Brutalist Command: art scene-index

This Short introduces no new sources. Every on-screen fact traces back
to the parent long's evidence chain, which is the isolated toolkit
itself.

## Parent evidence chain (unchanged)

- `../../claude-liam-brutalist-command-scene-index/SOURCES.md` —
  full landscape sources list.
- `../../claude-liam-brutalist-command-scene-index/FACTCHECK.md` —
  landscape claim ledger.
- `../../claude-liam-brutalist-command-scene-index/beat_sheet.json` —
  landscape 16:9 beat sheet with per-beat sources.
- `../../claude-liam-brutalist-command-scene-index/vertical/beat_sheet.json` —
  parent's native portrait companion (this Short is a whole-beat cut of it).

## Direct-source code paths (all inside the isolated toolkit)

- `runtime/scripts/build_scene_index.py` — the tool whose top-of-file
  comment is displayed verbatim in B03; the same tool whose bare
  invocation is captured verbatim in B05.
- `runtime/scripts/build_scene_index.py --todo` — the `--todo` flag whose
  outputs (`SCENE-DOC-TODO.md`) are named in the retained narration.
- `art` dispatcher lines 71–74 — the "no aliases" claim retained by B09
  line 5 (the source card listing was cut with B02, but the claim is
  preserved).
- `runtime/remotion/src/Root.tsx` — the file whose `<Composition>`
  registrations THE LAW (B03) points at.
- `runtime/remotion/src/scenes.json` — the index B05 shows on disk and
  B09 line 3 names in the surface summary.
- `SCENE-DOC-TODO.md` — the backfill ledger B05 shows on disk and B09
  line 4 names in the surface summary.

## Media inheritance

- All video clips (`media/B*.mp4`) — independent copies of the parent's
  native 2160×3840 vertical MP4s. No re-render.
- All narration (`mp3/beat-B*.mp3`) — independent copies of the parent's
  Kokoro `am_onyx` renders. No regeneration.
- No new pantry overrides, no fresh captures, no external stock, no
  paid API calls.
