# PROMPTS — Short (9:16) — Brutalist Command: art scene-index

This Short is a whole-beat cut of the parent's native portrait companion.
No prompts were regenerated. Composer beats reuse the parent's rendered
props verbatim.

## What appears on-screen (from the retained beats)

- **B00 composer ask** — greeting `Bonjour, Liam`; command line
  "After adding a Remotion component, re-read Root.tsx and regenerate the
  scene index the library-first search uses.", runningText
  "reading Root.tsx…", three output lines naming the wrapped script, the
  two output files, and THE LAW.
- **B01 hesitant writer** — the three-line BLUF "art scene-index / reads /
  the scenes." with triggerWords `reads, scenes` replaced by `rewrites,
  index`, on the parent's portrait-scale patched serif face.
- **B03 code card** — the `build_scene_index.py` header comment naming THE
  LAW and the search-surface follow-on; spark line "Registered is the
  only truth."
- **B05 code card** — the verbatim `./art scene-index` invocation and
  `ls -l` on the two output files; spark line "619 · 118 · 0. Two files
  on disk."
- **B09 verdict artifact** — eight one-liner rows summarising the whole
  surface of the command.
- **B11 outro** — `Brutalist Command: art scene-index.`, `@NikBearBrown`
  (locked handle), slug-seeded mascot.

## Local build for this Short

```bash
REEL=/Users/bear/Documents/CoWork/bear-textbooks/books/brutalist.art/youtube/brutalist/shorts/claude-liam-brutalist-command-scene-index-short
python3 runtime/scripts/compile.py "$REEL" \
    --height 3840 --out "$REEL/exports/short"
```

No `--review`. Output is `exports/short/claude-liam-brutalist-command-scene-index-short.mp4`
at 2160×3840, h.264 + AAC, no captions.

## Not regenerated

- No audio regeneration — narration MP3s are byte-identical copies of the
  parent's Kokoro `am_onyx` renders.
- No remotion re-render — every retained beat is a byte-identical copy of
  the parent's native 2160×3840 portrait MP4.
- No endcard PNG — `--no-endcard` was used; the Short ends on the outro
  card.
