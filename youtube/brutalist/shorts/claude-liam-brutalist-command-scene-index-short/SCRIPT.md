# SCRIPT — Short (9:16) — Brutalist Command: art scene-index

Whole-beat cut. Narration below is byte-identical to the parent's Kokoro
`am_onyx` renders (`../../claude-liam-brutalist-command-scene-index/vertical/mp3/`).
Six beats · ~105.6 s · no captions · no endcard.

## B00 — ASK (15.71 s) — ClaudeComposerAsk916

> Bonjour, this is Liam, in for Bear. Six hundred and twenty renderable
> compositions live in Brutalist. Art scenes searches that index. Art
> scene-index writes it. When you add a component to Root dot tsx, the
> search cannot see it until you re-index. This is the tool that closes
> that loop.

## B01 — BLUF (13.80 s) — BrutalistHesitantWriter916

> Watch the writer walk this back. Art scene-index does not read the
> scenes for you. It reads Root dot tsx and rewrites the index that art
> scenes searches. Search reads the index. Scene-index writes it. That
> is the whole distinction this reel turns on.

## B03 — MECHANISM · THE LAW (21.75 s) — ClaudeCodeBeat916

> Here is the law the tool exists to enforce. A scene is usable only if
> Root dot tsx declares a Composition for it. A dot tsx file sitting in
> scenes slash is not enough — several exist that no Composition
> references, and they are invisible to the pipeline. So the index is
> generated from Root dot tsx for that exact reason. Registered is the
> only truth. Everything else in the folder is a draft.

## B05 — WORKED EXAMPLE · verbatim run (21.96 s) — ClaudeCodeBeat916

> So the bare invocation. Dot slash art scene-index. It runs, it prints
> two lines and exits zero. Six hundred and twenty renderable
> compositions. One hundred and eighteen of them are running on derived
> search text. Zero unresolved — every registered id has a backing file
> on disk. Two files change on disk: scenes dot json under runtime slash
> remotion slash src, and SCENE-DOC-TODO markdown at the repo root.

## B09 — VERDICT (26.75 s) — ClaudeVerdictArtifact916

> So the surface. Bare art scene-index re-reads Root dot tsx and writes
> two files. Scenes dot json — the six hundred and twenty entry index
> that art scenes searches. SCENE-DOC-TODO markdown — the one hundred
> and eighteen entry backfill list from the todo flag baked into the
> wrapper. There are no aliases; the dispatcher matches only
> scene-index. And the law under all of it: a scene is renderable only
> if Root dot tsx declares a Composition for it. A file on disk is a
> draft. Root dot tsx is the truth.

## B11 — OUTRO (5.63 s) — ClaudeTitleOutro916

> Brutalist Command: art scene-index. Liam, in for Bear, for at
> Humanitarians A I.

## Transition read

- B00 introduces the split (scenes searches / scene-index writes) and
  names Root.tsx as the surface that closes the loop.
- B01 corrects the natural misreading — "reads the scenes" walked back
  to "rewrites the index".
- B03 states THE LAW verbatim: registered is the only truth.
- B05 lands the concrete run: 620 · 118 · 0, two files on disk.
- B09 restates the whole surface in a verdict artifact.
- B11 is the required outro; nothing was regenerated.

No dropped beat is referenced by the retained narration. The verdict's
line about "documented vs derived" is a self-contained one-liner; it
does not require the cut B06 comparison to make sense.
