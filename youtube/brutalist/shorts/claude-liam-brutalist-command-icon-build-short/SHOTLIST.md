# SHOTLIST — Brutalist Command: art icon-build — Short (9:16)

6 beats · Kokoro `am_onyx` · Claude fidelity palette · @HumanitariansAI folder chip · @NikBearBrown outro (locked)

Derived from `youtube/brutalist/claude-liam-brutalist-command-icon-build/vertical` by whole-beat cut. Every retained clip is the parent's native 2160×3840 portrait render, copied unchanged (no re-render, no crop). See `CUT-PLAN.json` for the editorial decisions.

| # | Beat | Scene / pattern | Duration | Notes |
|---|---|---|---|---|
| B00 | ASK — cold open | `ClaudeComposerAsk916` | 16.67 s | Merhaba, Liam. States the transform: 12 GB / 2.4 M ↦ 30 MB / 7,330 icons. |
| B01 | BLUF — hesitant writer | `BrutalistHesitantWriter916` | 14.58 s | Central misconception fix: `downloads`/`new` → `cuts`/`existing`. Subtractive, not additive. |
| B04 | MECHANISM — the four rules | `ClaudeCodeBeat916` | 25.92 s | Stroke only · one-per-thing · one geometry/weight · B&W (currentColor). |
| B06 | WORKED EXAMPLE — `--dry-run` | `ClaudeCodeBeat916` | 26.08 s | Verbatim per-source kept/dropped counts summing to 7,330. |
| B09 | VERDICT — the whole surface | `ClaudeVerdictArtifact916` | 24.46 s | 7 lines: bare invocation, `--dry-run`, `--limit N`, four rules, four exclusions, 44 pt math, NOTICE.md. |
| B11 | OUTRO — title restate | `ClaudeTitleOutro916` | 5.75 s | "Brutalist Command: art icon-build."; hardcoded `@NikBearBrown` (OUTRO-LOCK); slug-seeded mascot. |

Measured timeline: 113.5 s. Container / video / audio: 113.458 s / 113.417 s / 113.458 s. Strictly under 180 s.

## Dropped from the long

`B02` dispatcher case-branch · `B03` `du -sh` table · `B05` 44 pt math walkthrough · `B07` output-artifacts tree · `B08` exclusions catalogue · `B10` Your Turn. Each is preserved unchanged in the parent 9:16 and the 16:9 masters.

## Compiler SKIN-LINT expected notice

The compiler emits `SKIN-LINT: palette=claude but the cold open is 'ClaudeComposerAsk916'` and the equivalent for `ClaudeTitleOutro916`. These are false positives — the `*916` wrappers are the correct portrait variants of the Claude scenes.

## Visual language

Identical to the parent vertical (Claude fidelity — cream page `#FAF9F5`, warm ink `#3D3929`, terracotta spark `#D97757`). EB Garamond serif for greetings and titles; SF Mono for terminal/code lines.
