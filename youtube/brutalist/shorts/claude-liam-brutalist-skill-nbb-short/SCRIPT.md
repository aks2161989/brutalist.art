# SCRIPT — The nbb Skill — Short

Whole-beat cut. All narration below is the vertical parent's, unchanged.
Kokoro `am_onyx` audio was copied from the parent, not regenerated.

## B00 · ASK / cold open (16.29 s)

> Hola, this is Liam, in for Bear. Today we tear down the nbb skill. It is
> the one command that takes any reel or lecture and derives an NBB channel
> cut of it — a new nbb dash directory, a scaffolded beat sheet, and a
> Teardown register waiting to be written. Here is the mental model.

## B01 · BLUF / hesitant writer (19.38 s)

> The easy read — the one the name invites — is that nbb rewrites your
> reel. It does not. The nbb skill scaffolds an empty nbb dash container
> next to your source, presets the metadata for the NikBearBrown channel,
> and hands Claude a checklist. The narration rewrite, the LLM exercise,
> and the outro are Claude's work. The canonical source is never touched.

## B03 · FRAMEWORK / pipeline (34.75 s)

> The flow is five steps, and only the first is code. Scaffold — brand
> underscore variant dot py copies the beat sheet into a new nbb dash
> directory and presets audience, palette, register, engine, voice. That
> is the whole deterministic half. Then Claude does the rest. Rewrite
> every beat's narration in the Teardown register — take the machinery
> apart, judge the design choices. Insert one LLM exercise as the
> second-to-last beat — a paste-ready prompt and a dig-deeper follow-up.
> Append the NikBearBrown outro from AUTHOR dot md. Then verify the
> ending order: body, LLM exercise, outro. Same shape every reel.

## B09 · VERDICT / artifact card (34.25 s)

> The verdict. The nbb skill derives an NBB channel cut of any reel or
> lecture — new sibling directory, presets metadata, hands Claude a
> checklist. Source is never modified — the canonical beat sheet is
> byte-identical before and after. Two halves — deterministic scaffold in
> Python, creative rewrite in the Teardown register, guided by Claude.
> The second-to-last beat is always an LLM exercise — paste-ready prompt
> plus a dig-deeper follow-up. Voice is Kokoro am onyx, Liam in for Bear,
> IN dash FOR dash BEAR law. And the checklist is a checklist — brand
> underscore variant dot py never tries to be clever about voice.

## B11 · OUTRO (3.54 s)

> That was the nbb Skill. Liam, in for Bear.

## Total measured audio

**108.21 s.** Strictly under the 180 s cap with ~72 s of headroom.
