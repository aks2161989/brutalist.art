# PROMPTS.md — The nopunt Skill (Short)

The Short reuses the parent's per-beat renders and mp3s unchanged; only one
on-screen paste-ready prompt appears in the retained cut (the B00 cold open).
The parent's B10 handoff (Your Turn audit prompt) is dropped for the Short —
see the 16:9 long for the full paste-ready audit rubric.

## Cold open (B00) — paste-ready ask

Rendered inside `ClaudeComposerAsk916` as the visible composer command:

> What does the `nopunt` skill actually do? Read `skills/make/nopunt/SKILL.md`
> and tell me — the mental model, not the section headings.

Composer answer lines shown on screen:

1. the anti-punt catalog — one place to check before ANY placeholder
2. HOLD is only a genuine archival photograph — everything else animates
3. unfilled animatable content = PUNT (a bug, not a style choice)

## No other paste-ready prompt is on screen

- **B01** (BLUF, hesitant writer) — narrative correction, no user command shown.
- **B04 / B05 / B06 / B07** (Mechanism cards) — quote blocks are excerpts from
  `skills/make/nopunt/SKILL.md`, not prompts the viewer runs.
- **B11** (Outro) — title + handle only.

## Rendering / audio

- All Remotion renders: reused unchanged from `parent_reel/vertical/media/`.
- All narration mp3s: reused unchanged from `parent_reel/vertical/mp3/` (Kokoro `am_onyx`, Liam in for Bear). No paid API was called in this session.

## What is deliberately not here

- No new prompt authored for this Short.
- No compile-time overlay text (`--review` is NOT used for the final pass).
- No captions / subtitle track — Brutalist Shorts render without burned-in captions.
