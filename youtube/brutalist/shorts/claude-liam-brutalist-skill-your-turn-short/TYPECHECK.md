# TYPECHECK — The your-turn Skill (Short)

Run ID: `bfb6c15f19af454ab8a8e43c589e1cbf` · 2026-09-12

## Legacy type checker

`runtime/scripts/type_check.py` and `reference/type-spec.md` are still absent
from the repository (unshipped). No fabricated pass is claimed. Where the
absent tool would run, the actual visual review below stands in.

## Actual typography review (24 frames × 2160×3840)

Every retained beat is a native portrait Remotion render byte-copied from the
parent. No character was re-typeset in this Short. Confirmed for each beat via
`_qc/frames/<bid>-p{15,50,85}.jpg`:

| Beat | Pattern | Type check |
|------|---------|-----------|
| B00  | ClaudeComposerAsk916    | Poster serif greeting "Hallo, Liam" upper-case-safe; composer body monospace at legible display size; three RESULT lines in terracotta comment tone; footer chip @HumanitariansAI clean. |
| B01  | BrutalistHesitantWriter916 | Serif face; 4-line stanza; `fontSize 145 lineSpacing 2.7`. Portrait BLUF coverage well ≥55 % of safe area at 50/85 %. Final stanza reads "your-turn is / not a / closing-block / skill.". |
| B03  | SkillTeardownMechanism916 | Eyebrow SKILL · BLOCK small caps; heading "Three beats. In order." poster serif; body sans at reading size; monospace quote card with terracotta rule; pill "THREE-BEAT BLOCK · IN ORDER". |
| B09  | SkillTeardownMechanism916 | Same pattern; "One fixture. Zero errors." heading; report line renders "changed=1, skipped=0, needs_prompt=0, errors=0" cleanly at monospace-adjacent size. |
| B10  | SkillTeardownMechanism916 | "What your-turn does NOT do." heading; three numbered limits typeset in body; grey (non-positive) pill "DOCUMENTED LIMITATION". |
| B11  | ClaudeVerdictArtifact916 | Card frame ~84 % width per fix folded into the skill; artifactTitle "The your-turn Skill"; heading "The your-turn split."; six numbered lines, no leading-number collision (transformer stripped; component defends). |
| B12  | ClaudeComposerAsk916    | Same composer template; greeting "Your turn."; longer paste-ready command wraps 8 lines and fits inside the composer body; three viewer-check lines in terracotta comment style. |
| B13  | ClaudeTitleOutro916     | Poster serif "The your-turn Skill" with the terracotta period; "@NikBearBrown" handle below in small caps; seeded mascot glyph anchored bottom. OUTRO-LOCK compliance flagged in parent metadata. |

## Kerning / bleed

No kerning defect observed. No character crosses the portrait safe area at
any of the 24 sample frames. The Mechanism916 pattern is used instead of the
horizontal Pipeline strip in B03, as per feedback memo
`skillteardown-pipeline-portrait`; the Pipeline horizontal phase strip that
would have bled past `SAFE916` left/right is not present in this cut.

## Conclusion

Typography passes visual review on every retained beat. No blockers or majors.
