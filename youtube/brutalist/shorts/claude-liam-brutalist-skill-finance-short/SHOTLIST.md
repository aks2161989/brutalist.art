# SHOTLIST — The finance Skill — Short

Seven kept beats, native 9:16 (2160×3840), one visual per beat. Registered
Remotion scenes only. Cream page, warm ink, terracotta accent per Claude
brand. Reused unchanged from the parent portrait renders — no re-render
was needed (the shorts.py `native portrait reused unchanged` path fired for
every retained beat).

| # | Scene | Motion | Duration ⌛ |
|---|---|---|---|
| B00 | `ClaudeComposerAsk916` | type-on composer + result output | 15.03s |
| B01 | `BrutalistHesitantWriter916` | typed text with correction on "renders" | 14.70s |
| B04 | `SkillTeardownMechanism916` | heading + body + quote + verdict pill | 26.70s |
| B05 | `SkillTeardownMechanism916` | heading + body + quote + verdict pill | 37.10s |
| B06 | `SkillTeardownMechanism916` | heading + body + quote + verdict pill | 36.90s |
| B09 | `ClaudeVerdictArtifact916` | artifact card, five-line verdict | 36.03s |
| B11 | `ClaudeTitleOutro916` | poster serif title + terracotta period + @NikBearBrown | 3.40s |

Total render_duration_s = 169.86s (measured from copied per-beat MP4s).
Container ffprobe reports 169.917s. Under 175s target; strictly under 180s.

## Dropped for the Short (see the 16:9 long)

| # | Scene | Reason |
|---|---|---|
| B02 | `SkillTeardownAnatomy916` | Generic toolkit anatomy — long-form detail |
| B03 | `SkillTeardownPipeline916` | Detailed 11-beat spine — template plumbing |
| B07 | `SkillTeardownMechanism916` | Colour + geometry decision — verdict summarises |
| B08 | `SkillTeardownMechanism916` | Bank/insurer/REIT falsifiability — verdict summarises |
| B10 | `ClaudeComposerAsk916` | Extended Your Turn planning prompt — long CTA |

## `shot.show` events per beat

See `beat_sheet.json` for the full `shot.show` array on each retained beat.
Copied verbatim from the parent portrait beat sheet; no visual re-timing.

## SPARK-LINE compliance

- B04 — "Locked shapes. No choice."
- B05 — "Never retyped. Ever."
- B06 — "Two audits. Both fail."

## ILLUSTRATE LAW compliance

The Claude interface appears only at B00 (cold open) and B11 (title outro).
The three retained body beats (B04/B05/B06) are concept illustrations on
cream — no composer wallpaper. B09 is a verdict artifact card.
