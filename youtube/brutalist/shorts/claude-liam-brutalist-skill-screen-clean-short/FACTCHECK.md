# FACTCHECK — The screen-clean Skill — Short

Every factual claim in the six retained beats traces to
`skills/make/screen-clean/SKILL.md` in the isolated toolkit. No claim is
invented and no result of a run is asserted that was not observed in the
parent long's build. This Short reuses the parent's narration audio and
native portrait renders unchanged; it does not re-execute the demo.

## B00 — ASK · cold open

- "shared preprocessor" · "Zoom, Teams, Meet, or QuickTime" — SKILL.md
  frontmatter: "Prepare ANY screen recording (Zoom, Teams, Meet, QuickTime)
  for use as a beat inside a reel."
- "Five passes." — five `## Pass N` headings in SKILL.md.
- "One review artifact." — SKILL.md § Output contract lists the contact-sheet
  path as one of the returned items; § Pass 2 states "the proof is the review
  artifact, not the numbers".
- "Never publishes." — SKILL.md frontmatter's final sentence.

## B01 — BLUF · hesitant writer

- "The name reads like a cleaner. It is not one." — pedagogy on doctrine;
  SKILL.md never uses "cleans" in the sanitizing sense.
- "does not sanitize your desktop or decide what is embarrassing on your
  behalf" — paraphrase of § Pass 4: "'is this embarrassing' is judgment, not
  arithmetic".
- "safe defaults" — § Pass 2 selects route B as the default and applies it
  under `--silent`.
- "flags what a human still needs to look at" — § Pass 4: "wait for a human".
- "The proof … is the contact sheet at the end." — § Pass 2: "the proof is
  the review artifact, not the numbers".

## B03 — FRAMEWORK · five passes

- Pass 1 "probe the file and decode the audio" — § Pass 1 headings.
- Pass 2 "aspect fit … measured against the taskbar, padded with cream bars,
  never cropped from the top" — § Pass 2: "Measure the taskbar. Do not assume
  it." + "Bars are Claude cream `#FAF9F5`, never black." + "never crop from
  the top".
- Pass 3 "legibility, sampled from real frames at output size" — § Pass 3:
  legibility sampled at output size, illegibility is a finding.
- Pass 4 "privacy scan, which flags every incidental thing on screen and
  waits for a human" — § Pass 4 title and body.
- Pass 5 "dead dash air trim on the ends only. Middle edits are the caller's
  job." — § Pass 5: leading + trailing silence only; middle edits belong to
  the caller.

## B07 — MECHANISM · Pass 4 flag-never-decide

- Item list ("clock, dock icons, browser tab titles, notification toasts,
  filenames, calendar previews") — SKILL.md § Pass 4 enumerated items.
- "marks which ones the taskbar crop already removes, and which ones survive"
  — § Pass 4: "mark which items **survive the crop** and which the crop
  removes".
- "then it waits" — § Pass 4: "wait for a human".
- "is this embarrassing is a judgment call, not arithmetic" — direct
  paraphrase of § Pass 4 doctrine sentence quoted on the mechanism card.
- "matters more for fellows … than for board members who present
  deliberately" — § Pass 4 note on personal-machine fellows vs. deliberate
  board-member captures.

## B09 — VERDICT · artifact card

Six verdict lines, each a re-statement of the passes already introduced.

- "shared preprocessor for any skill that drops a recording into a reel" —
  SKILL.md § Trigger.
- "deliberately ignorant of who is speaking. Format is orthogonal to
  relationship." — SKILL.md § "Why it is shared, not built into one skill".
- "Pass one is a gate — mean volume below negative forty and the build stops"
  — § Pass 1: "If mean_volume is below -40 dB, STOP."
- "Pass two crops the taskbar, pads the rest with cream, and never touches
  the top" — § Pass 2 default and no-top-crop rule.
- "Pass four flags what is on screen and waits for a human" — § Pass 4.
- "cleaned nineteen twenty by ten eighty video plus a contact sheet" —
  § Output contract.

## B11 — OUTRO · sign-off

- Title restate and Liam sign-off per OUTRO LAW / OUTRO-LOCK. Handle
  `@NikBearBrown` is the outro-locked publisher.

## Claims deliberately not made in this Short

- No specific taskbar-pixel or bar-width numbers (72 px / 56 px) — those live
  in dropped beat B06.
- No `-91.0 dB` / `-21.1 dB` fixture measurements — those live in dropped
  beat B05.
- No four-frame 10 / 35 / 60 / 85 % sampling detail for the contact sheet —
  that lives in dropped beat B08.
- No repository folder layout — that lives in dropped beat B02.
- No paste-ready dry-run prompt — that lives in dropped beat B10.

All are still present in the parent long and are correct as stated there;
this Short simply does not narrate them.
