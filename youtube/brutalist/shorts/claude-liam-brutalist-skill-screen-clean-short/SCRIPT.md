# SCRIPT — The screen-clean Skill — Short (retained beats)

Derivative of the vertical long
`claude-liam-brutalist-skill-screen-clean-vertical`.
Whole-beat cut, no rewrites. Six beats kept in source order; parent narration
files reused unchanged. Voice: Kokoro `am_onyx` (Liam, in for Bear).
Total measured: **128.125 s** (container + video + audio).

---

## B00 — ASK · cold open (~16.0 s)

> Bonjour, this is Liam, in for Bear. Today we tear down the screen dash clean
> skill. It is the shared preprocessor that turns a screen recording — Zoom,
> Teams, Meet, or QuickTime — into a beat that fits inside a reel. Five passes.
> One review artifact. Never publishes. Here is the mental model.

## B01 — BLUF · hesitant writer, `auto-blurs` → `flags` (~18.6 s)

> The name reads like a cleaner. It is not one. The screen dash clean skill
> does not sanitize your desktop or decide what is embarrassing on your
> behalf. It runs five passes over the recording, applies safe defaults, and
> flags what a human still needs to look at. The proof of a good pass is not
> the number of pixels cropped. It is the contact sheet at the end.

## B03 — FRAMEWORK · the five passes (~26.6 s)

> The flow is five passes, and only one of them ever decides anything on your
> behalf. Pass one — probe the file and decode the audio. Pass two — aspect
> fit, measured against the taskbar, padded with cream bars, never cropped
> from the top. Pass three — legibility, sampled from real frames at output
> size. Pass four — privacy scan, which flags every incidental thing on
> screen and waits for a human. Pass five — dead dash air trim on the ends
> only. Middle edits are the caller's job, not this one's.

## B07 — MECHANISM · Pass 4, flag never decide (~32.1 s)

> Fourth decision, and this is the line the skill will not cross. Pass four
> is a privacy scan. screen dash clean lists every incidental thing it sees —
> clock, dock icons, browser tab titles, notification toasts, filenames,
> calendar previews. Then it marks which ones the taskbar crop already
> removes, and which ones survive into the reel. Then it waits. It does not
> auto blur. It does not decide. Is this embarrassing is a judgment call, not
> arithmetic. And the doctrine is explicit — this matters more for fellows,
> who are on personal machines with their own life on screen, than for board
> members who present deliberately.

## B09 — VERDICT · artifact card (~30.8 s)

> The verdict. screen dash clean is the shared preprocessor for any skill
> that drops a recording into a reel. It is deliberately ignorant of who is
> speaking. Format is orthogonal to relationship. Pass one is a gate — mean
> volume below negative forty and the build stops. Pass two crops the
> taskbar, pads the rest with cream, and never touches the top of the frame.
> Pass four flags what is on screen and waits for a human. And the
> deliverable back to the caller is a cleaned nineteen twenty by ten eighty
> video plus a contact sheet you can actually look at. Small skill. Sharp
> job.

## B11 — OUTRO · Liam sign-off (~4.0 s)

> That was the screen dash clean Skill. Liam, in for Bear.

---

## Dropped in this cut

Kept in the parent long (`youtube/brutalist/claude-liam-brutalist-skill-screen-clean`):

- **B02** — on-disk anatomy of `skills/make/screen-clean/`.
- **B04** — mechanism act 1: shared preprocessor rationale (ignorant of speaker).
- **B05** — mechanism act 2: Pass 1 audio-gate demo with `-91.0 dB` / `-21.1 dB` fixtures.
- **B06** — mechanism act 3: Pass 2 A/B/C aspect-fit table (cream vs. black bars, never-crop-top).
- **B08** — falsifiability: four-frame contact sheet at 10/35/60/85 %.
- **B10** — Your Turn dry-run homework.
