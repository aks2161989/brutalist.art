# SCRIPT — Download Brutalist with Claude Code — Short

A 9:16 native portrait Short cut from the parent vertical (10 beats → 6 beats). Six beats retained unchanged. Narration and per-beat renders reused verbatim; nothing regenerated.

- Kind: `short` · Aspect: 9:16 · Dimensions: 2160×3840
- Voice: Liam (in for Bear), Kokoro `am_onyx`, free
- Palette: Claude fidelity (cream `#FAF9F5`, ink `#3D3929`, terracotta `#D97757`)
- Channel: `@HumanitariansAI` · Playlist: Brutalist
- Captions: false · Publishes: never

## Retained beats (in order)

### B00 · ASK — `ClaudeComposerAsk916` (12.99s)

> Ciao. This is Liam, in for Bear. You already have Claude Code. So downloading Brutalist is one paste into the composer. Not a wizard, not a package. A git clone, a free doctor script, and a readiness table. Here is what actually happens.

Cold open on the Claude composer. Greeting `Ciao, Liam`. The ask types itself. Three output lines settle underneath: the `git clone`, the `./setup --install`, and the plain `./setup` readiness table.

### B04 · WORKED EXAMPLE · SETUP DOCTOR — `DbcDoctor916` (20.57s)

> Now run the doctor. Dot slash setup dash dash install once, to pull the free dependencies. Then plain dot slash setup, over and over, to see the readiness table. Green means the feature is live. Red means one dependency is missing, and the doctor tells you exactly which install command to run. It is the same script the sandbox in this reel just executed — the readings on screen are real.

A terminal card opens with `$ ./setup`. Seven feature rows arrive (`audio (Kokoro)`, `captions`, `Manim beats`, `Manim equations`, `Remotion beats`, `slates`, `fonts`), each green. Closing green line: `All core features ready. Cost: $0.00.`

### B05 · FRAMEWORK · SUBSCRIPTION vs API — `DbcLanes916` (21.55s)

> This part trips fellows up, so read it slowly. Your Claude Code subscription is what lets the agent think and write. That is one lane. A paid Anthropic API key is a different lane — optional, metered, only needed if you script Claude outside the desktop app. Brutalist itself doesn't spend a cent. Kokoro is a local voice model. Remotion renders on your machine. Manim runs on your machine. Free by default — that is the shape of this tool.

Three lanes light up in order: `CLAUDE CODE SUBSCRIPTION` · `PAID ANTHROPIC API` (optional in terracotta) · `BRUTALIST PIPELINE` with Kokoro / Remotion / Manim rows. Banner underneath: `Setup spends $0.00.`

### B06 · FALSIFIABILITY — `DbcNotClaims916` (19.20s)

> Now the honest limits. Setup does not sign you into anything — no accounts are created. Setup does not touch any file outside the folder you cloned into. Setup does not upload. It does download one thing from the public internet the first time you run install — the Kokoro voice model, about three hundred forty megabytes, straight from a GitHub release. That is the whole network footprint.

Three rows arrive: **No accounts created.** · **No files outside this folder.** · **No upload. One download** (~340MB Kokoro voice model).

### B07 · VERDICT — `DbcVerdict916` (11.48s)

> Verdict, in three lines. Clone the repo — you have the toolkit. Run the doctor — you know exactly what is ready. Keep the free lane — the pipeline itself never asks for a card. That is how you download Brutalist.

Artifact card. Three terracotta-numbered lines. Sparkline "That is how you download Brutalist." at the bottom.

### B09 · OUTRO — `DbcOutro916` (5.91s)

> That was Download Brutalist with Claude Code. Liam, in for Bear. For Humanitarians AI.

Poster serif title restate with the terracotta period. **@HumanitariansAI** handle beneath. `LIAM, IN FOR BEAR.` subline.

## Dropped from the long (recorded in `CUT-PLAN.json`)

- **B01** — HesitantWriter misconception (`installer` → `git clone + doctor`). B00's cold open already carries the reframe ("Not a wizard, not a package. A git clone, a free doctor script, and a readiness table.").
- **B02** — Paste-ready composer command. Duplicates B00's cold-open ask.
- **B03** — File-tree anatomy (`setup`, `art`, `skills/`, `runtime/`, `docs/`, `brands/`, `README.md`). Repository internals belong in the 16:9 long.
- **B08** — Your Turn composer re-type. B09 signs off with the invitation implicit in "Liam, in for Bear. For Humanitarians AI." A viewer who wants the paste-ready prompt can find it in the long.

Total planned run time: 91.79s (well below the 180s Shorts cap; well below the 175s planning ceiling).
