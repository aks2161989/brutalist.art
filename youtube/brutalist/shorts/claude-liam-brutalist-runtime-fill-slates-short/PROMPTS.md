# PROMPTS — Brutalist Utility: fill_slates.py (Short)

Only one composer card survives the Short cut (B00). The parent's YOUR TURN
composer (B10) was dropped per the Shorts prompt's explicit permission —
Shorts need not read the long teardown's full exercise aloud.

## Composer prompt on-screen — B00, the ask (cold open)

Greeting: `Hej, Liam`
Segment: `fill_slates.py`
Topic: `BRUTALIST · UTILITY: FILL_SLATES.PY`
Folder chip: `@HumanitariansAI`
Model / effort chips: `Claude` / `Sonnet`

Command shown in composer (typed live in B00):

> Three beats across two reels have no video and no remotion pattern. Fill
> them all with a Form A card so the review cuts play through. Do NOT touch
> the beats that are already good.

Running text: `invoking fill_slates.py…`

Cascade output lines:

1. `walk books/ — find every reel with a beat_sheet.json`
2. `for each SLATE beat (no media, no pattern, not silent) — stamp FormACard`
3. `then shell out to remotion_scenes.py + run.sh to recut the review reel`

## Why the Short omits YOUR TURN

The Shorts contract explicitly permits omitting the parent's YOUR TURN when a
coherent whole-beat cut is shorter without it. The Short's B09 verdict already
lands the whole surface in the order the tool runs; ending on the OUTRO card
(B11) closes cleanly at 123.8 s. Fellows who want the extended assignment
should watch the full-length teardown, which retains the YOUR TURN composer
verbatim.

## No new URLs, no invented handles

- Handle in outro: `@NikBearBrown` (OUTRO-LOCK inherited from the parent;
  flagged in the parent's `README.md` for `@HumanitariansAI` human review
  before publication — this Short adds no new claim about that handle).
- Channel chip: `@HumanitariansAI` (matches the parent).
- No public URL of an as-yet-unpublished 16:9 long is invented or referenced;
  the Short stands on its own within the Brutalist playlist.
