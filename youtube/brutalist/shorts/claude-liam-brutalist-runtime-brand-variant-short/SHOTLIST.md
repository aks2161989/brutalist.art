# SHOTLIST — Brutalist Utility: brand_variant.py — Short

This is the 9:16 Short cut of the long ai-explainer teardown. Six beats retained
from the parent's twelve; native-portrait renders reused unchanged (all sources
are 2160×3840 remotion mp4s). Motion and props inherit the parent shot cards.

| Beat | Act | Scene component (used) | Kind | What appears (before narration lands) |
|------|-----|------------------------|------|----------------------------------------|
| B00 | ASK | `ClaudeComposerAsk916` | UI | Composer card fades in on cream; `Yassou, Liam` types above; the ask fills the composer; running text `invoking brand_variant.py…` appears; three output lines cascade under the composer. |
| B01 | BLUF | `BrutalistHesitantWriter916` | Illustration | Cream page holds; the serif line types `the brand.py / variant just / sits and / rewrites the / narration.`; `rewrites` turns terracotta, backspaces, retypes `stamps`; `narration` turns terracotta, backspaces, retypes `metadata`; final settle. |
| B02 | FRAMEWORK | `SkillTeardownMechanism916` | Illustration | Eyebrow `UTILITY · PIPELINE`; heading `The audience-variant scaffold — deterministic half only.`; body `Reel + audience → RESOLVE → STAMP → TODO → EMIT` with the brand_variant.py:13-16 quote; spark chip `ONE CANONICAL · THREE AUDIENCES · ONE VARIANT AT A TIME`; spark line. |
| B04 | WORKED_EXAMPLE | `ClaudeCodeBeat916` | Code | Card titled `demo/hai-canonical/beat_sheet.hai.json (verbatim)`; the two `[variant]` status lines type; the emitted `.metadata` block types row by row (audience, register, palette, engine, voice_kokoro, typography, channel_title, `_variant_todo`); spark line `Deterministic metadata. Creative rewrite deferred.` |
| B09 | VERDICT | `ClaudeVerdictArtifact916` | Illustration | Artifact card `Verdict`; heading `brand_variant.py — the whole surface, in the order it does its job`; six bullet lines cascade (input · lookup · resolve · stamp · todo · emit). |
| B11 | OUTRO | `ClaudeTitleOutro916` | Card | Cream page in; title types `Brutalist Utility: brand_variant.py.`; handle `@NikBearBrown` (locked, per OUTRO-LOCK.md) appears; slug-seeded mascot animates. |

## Dropped from the long

- B03 (CLI --help surface) — full argparse listing belongs in the 16:9 long.
- B05 (AUD lookup table) — 15-line configuration; the worked example B04 already names the audience keys and voices.
- B06 (`get_brand_dir()`) — reel vs lecture routing; not needed to state that a sibling folder is created.
- B07 (`main()` body walk) — line-by-line internals belong in the long.
- B08 (refusal cases + --force) — secondary example; the verdict already lists the safety rule.
- B10 (Your Turn) — long-form ADDED/CHANGED/POPPED exercise dropped; the ask in B00 and the verdict in B09 carry the "what to do next" beat.

## Aspect-specific notes

All six kept beats are native 2160×3840 remotion renders reused as independent
copies (no re-render, no crop). `SkillTeardownMechanism916` at B02 is already
the portrait swap for the horizontal `SkillTeardownPipeline` (per memory
`feedback_skillteardown_pipeline_portrait.md`).

The outro card is the locked `ClaudeTitleOutro916` with hardcoded `@NikBearBrown`
handle. Adaptation for @HumanitariansAI is flagged for the human reviewer in
BRIEF.md before publication; this Short does not publish.
