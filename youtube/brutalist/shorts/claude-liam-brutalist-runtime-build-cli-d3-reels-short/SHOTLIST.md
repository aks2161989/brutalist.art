# SHOTLIST — Brutalist Utility: build_cli_d3_reels.py — Short

Six retained beats out of the parent's twelve, 9:16 native (2160×3840), 96.96s
total. All portrait media (`media/B**.mp4`) copied unchanged from the parent's
`vertical/media/`; no beat re-rendered; no beat center-cut. Every beat below is
the parent's own `*916` Remotion render.

| # | Act | 916 pattern | Motion | What is on screen |
|---|---|---|---|---|
| B00 | ASK (Cold Open) | ClaudeComposerAsk916 | type-on | Composer card on cream. Greeting `Bula, Liam` serif; ask types in; runningText `invoking build_cli_d3_reels.py…`; three output lines cascade under the composer (config schema · beat-sheet stamp · pipeline sequence). |
| B01 | BLUF | BrutalistHesitantWriter916 | hesitant-type | 5-line stanza starts as `the tool / builds / a D3 sim / from / one prompt.`; `builds` swaps to `wraps`; `prompt` swaps to `HTML`; final sentence settles as `the tool / wraps / a D3 sim / from / one HTML.` fontSize 150, lineSpacing 2.25. |
| B02 | FRAMEWORK | SkillTeardownMechanism916 | phase-flow | Eyebrow `UTILITY · PIPELINE`; heading `One config in. N review cuts out. One row each in the log.`; body naming the STAMP → CAPTURE → AUDIO → REMOTION → COMPILE pipeline; verbatim source quote from `build_cli_d3_reels.py:14-20`; terracotta verdict chip `IDEMPOTENT PER SIM — A >50KB SLATE MP4 SHORT-CIRCUITS BACK TO SKIP.`; spark line `The tool WRAPS. It does not invent.` |
| B07 | WORKED EXAMPLE | ClaudeCodeBeat916 | code-cascade | Title `demo/config.json → make_beat_sheet() (verbatim)`; the config row (slug `00-tiny-wave`, book `demo-fixture-book`, note verbatim); dry-run output (`greeting_for('00-tiny-wave')='Yo'`, `make_title=…`, `extract_code head`); then the 10-beat stamped list `B00..B09` with their patterns. |
| B09 | VERDICT | ClaudeVerdictArtifact916 | artifact-in | Title `Verdict`; heading `build_cli_d3_reels.py — the whole surface, in the order it does its job`; six numbered lines: input · stamp · quote · shell out · log · recover. |
| B11 | OUTRO | ClaudeTitleOutro916 | outro-card | Title restated: `Brutalist Utility: build_cli_d3_reels.py.` with terracotta period; handle `@NikBearBrown` (locked); slug-seeded mascot below the handle. |

## Constraints applied to the Short cut

- SHORTS LAW: measured 96.96s, strictly under 180s.
- COLD OPEN LAW retained (B00 ClaudeComposerAsk916).
- OUTRO LAW / OUTRO-LOCK retained (B11 ClaudeTitleOutro916, `@NikBearBrown`).
- No parent visual altered; no scene re-rendered; no scene-source overlay used.
- No burned-in captions; no subtitle track; native portrait 2160×3840.
- HANDOFF LAW: dropped with B10 per shorts guidance — a detailed CTA can be cut.
- Second-beat HesitantWriter (B01) retained because it is this reel's misconception hook and lands in 11.17s.
