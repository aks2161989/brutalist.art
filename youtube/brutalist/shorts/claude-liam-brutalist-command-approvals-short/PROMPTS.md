# PROMPTS — Brutalist Command: art approvals — Short (9:16)

This Short does not call any generative image or video models. All kept beats are Remotion scenes rendered on the parent's `vertical/` build and reused here as independent copies. The only on-screen prompt is the cold-open ASK.

## B00 — the cold-open ASK typed into the composer

> Before I sign anything on a fellows reel, show me exactly what I would be
> signing. Do not sign it for me. Just print the fingerprints.

Runs on top of `ClaudeComposerAsk916`. Segment label `art approvals`, greeting `Selam, Liam`, folder chip `@HumanitariansAI`. Output lines cascade under the composer:

- prints two subjects (voice + professor_notes) with a SHA-256 each
- validates a pasted `metadata.approvals` block against those SHAs
- writes zero files — audit only, the pen never leaves the human

## Dropped prompt · B10 Your Turn

The parent film's Your Turn composer (a full paste-into-Claude-Code exercise built around `./art approvals <reel> --fingerprints`, the five-field record shape, and the `reviewed_by` field an agent must never fill in) is NOT in this Short. Viewers who want the exercise are served by the verdict card (B09) and the full 16:9 long. This is the cut, not a summary.

## Beat-prefixed instruction cards

None. All visuals are Remotion compositions; code cards are typed strings; hesitant-writer trigger words (`signs`, `files`) and replacements (`inspects`, `subjects`) are literal props. No image or video generation is invoked in the Short build path.
