# PROMPTS.md — The hai Skill (Short)

The paste-ready prompts shown or referenced in the RETAINED Short. Prompts
attached to dropped beats (B10) are not part of the Short — see the parent
reel's `PROMPTS.md` for those.

## B00 — Cold-open composer (shown on screen)

```
What does the `hai` skill actually do to a reel? Read skills/make/hai/SKILL.md and tell me — I want the mental model, not the README summary.
```

Expected 3-line output rendered under the composer:
- forks the reel into a new hai- directory
- rewrites the register to Plain + adds a CLI exercise
- source beat_sheet.json is never modified

## Verbatim SKILL.md quotes shown on screen

These are quoted (not paste-ready commands) inside the retained Mechanism
and Falsifiability beats. Each is line-cited in `FACTCHECK.md`.

- B04: "Source files (beat_sheet.json, build scripts, media) are never
  modified." — `hai SKILL.md` Standing rules.
- B05: "Required: when NOT to and where it fails — the AI main event for
  busy practitioners. This is not hedging; it is the diagnostic the audience
  needs. Do not skip." — `hai SKILL.md` Step 2 · Rewrite the register (Plain).
- B08: "There is no paid voice. ElevenLabs was permanently removed on
  2026-09-03." — `brands/hai.md · Voice`.

## Not in the Short

- The full "Your Turn" dry-run prompt (parent's B10) is dropped from this
  Short. Viewers are pointed to the 16:9 long for the exercise.
- The five-part CLI-exercise schema (parent's B06) is deferred to the long;
  B09's verdict retains the one-line "must genuinely run" rule.
- The batch-mode shell block (parent's B07) is deferred to the long.
