# PROMPTS — The explainer Skill · Short (9:16)

## Composer prompts SEEN ON SCREEN (retained beats)

### B00 — cold-open ASK (what the ask is)

```
What is the `explainer` skill? Read skills/make/explainer/SKILL.md and tell
me what it actually is.
```

Running text: `reading skills/make/explainer/SKILL.md…`

Output lines that land after send:
- it is a compositing PIPELINE, not a style
- two-axis shot system: type × source
- the still must be evidence, not texture

Note: B09 (the paste-ready `slate cut` Your Turn CTA) was dropped from this Short per
REPOLOOP-SHORTS-PROMPT §4 (a Short doesn't need to read the full exercise aloud). The B00
cold open's three output lines encapsulate the skill's shape for Shorts viewers.

## Prompts NOT shown on screen but used to build the reel (internal only)

None of these appear inside the film; recorded here for build reproducibility.

- Cut selection — chose whole-beat cut retaining B00 · B01 · B03 · B07 · B08 · B10
  (6 of 11 beats, ~116.9 s). Dropped B02, B04, B05, B06, B09. See `CUT-PLAN.json`.
- Trigger word swap (B01 BrutalistHesitantWriter) — single-token per contract:
  `style → pipeline`. The corrected sentence stands alone as the reel's
  claim ("The explainer skill is a pipeline. Sources never match. The
  treatment does.").
- B07 content — reused directly from parent vertical's 2026-09-11 rebuild;
  no new prompt generated for the EXECUTABLE-EVIDENCE.md sharpening.
