# PROMPTS.md — The anthropics Skill

*Every prompt or command shown on screen or run behind the reel. No paid API
was called; no external service was contacted; the only demos are file reads
and static toolkit inspection.*

## On-screen prompts (shown as Claude Composer beats)

### B00 · Cold open (composer, typed into `ClaudeComposerAsk`)

```
Tear down the anthropics skill. What does it actually do, not just what it says?
```

Result lines shown under the composer (three, on-brand):

- `reads the artifact, not the docs`
- `four modes: --repo, --paper, --content, --capability`
- `publishes never.`

### B09 · Your Turn (composer, typed into `ClaudeComposerAsk`)

```
Use the anthropics skill in --capability mode. Compare Claude with one other
model on a real task from my own work — a real prompt, real file, dated bench
log. Then answer the three review questions from the SKILL file. If any
answer is no, report the gap — don't build the episode.
```

Running text under the composer: `paste this into Claude Code…`

Viewer's success rubric (shown as output lines):

1. expected: one spine beat visible only by running the artifact
2. expected: every claim cited to file:line or on-screen output
3. expected: matching claims allowed to survive — no manufactured gap

## Local demos actually run (recorded in `demo/`)

The `anthropics` skill's real demo would require pointing it at real target
artefacts and running its sibling skills (`git-explainer`, `ai-paper`). Those
sibling skills are not present in this toolkit snapshot (see
`FACTCHECK.md · B07`). The reel does not fabricate a demo. It DOES record,
under `demo/`, exactly what CAN be verified locally about the SKILL file
itself. This is the source-backed local demonstration the BUILD-PROMPT
allows for capabilities that aren't runnable.

- `demo/01_sha_and_lines.sh` — records the SHA-256 and line count that the
  narration cites (used to justify the "142 lines" claim in B02).
- `demo/02_referenced_siblings.sh` — records whether the two dispatched-to
  sibling skills exist inside this toolkit's `skills/make/` (they don't).

Both scripts are recorded here as text with the observed output beside them.
No script uses network, credentials, or any paid API. Any command that would
otherwise reach outside the toolkit is skipped and labelled.

## Kokoro TTS (audio)

The narration was generated by the isolated toolkit's
`runtime/scripts/generate_audio_kokoro.py`:

```
python3 runtime/scripts/generate_audio_kokoro.py \
  /Users/bear/Documents/CoWork/bear-textbooks/books/brutalist.art/youtube/brutalist/claude-liam-brutalist-skill-anthropics
```

Voice: `am_onyx`. Model: local `kokoro-v1.0.onnx` + `voices-v1.0.bin` under
`$KOKORO_MODEL` / `$KOKORO_VOICES`. Cost: `$0.00`. No account, no key.

## Remotion renders (foreground, concurrency 1)

Rendered ONLY via the sanctioned entry point:

```
ART_CHROME=<toolkit>/runtime/chrome-wrapper.sh \
ART_CHROME_MODE=chrome-for-testing \
python3 runtime/scripts/remotion_scenes.py <reel>
```

The wrapper delegates to `chrome-headless-shell` with `--single-process
--in-process-gpu` so the render survives the App Sandbox's Mach IPC
restriction. The Remotion API and pipeline are not bypassed.

## Compile — 4K landscape and 4K portrait

```
python3 runtime/scripts/compile.py \
  /Users/bear/Documents/CoWork/bear-textbooks/books/brutalist.art/youtube/brutalist/claude-liam-brutalist-skill-anthropics \
  --height 2160 \
  --out /Users/bear/Documents/CoWork/bear-textbooks/books/brutalist.art/youtube/brutalist/claude-liam-brutalist-skill-anthropics/exports/landscape

python3 runtime/scripts/compile.py \
  /Users/bear/Documents/CoWork/bear-textbooks/books/brutalist.art/youtube/brutalist/claude-liam-brutalist-skill-anthropics/vertical \
  --height 3840 \
  --out /Users/bear/Documents/CoWork/bear-textbooks/books/brutalist.art/youtube/brutalist/claude-liam-brutalist-skill-anthropics/exports/vertical
```

Both compiles run the built-in Gate V, Gate T (if the type checker is
present), and atomic verification. `--review` is NOT a final; the commands
above are the finals.
