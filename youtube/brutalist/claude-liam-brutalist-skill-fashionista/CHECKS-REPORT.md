# Checks report — landscape

Reel: `The fashionista Skill` — `claude-liam-brutalist-skill-fashionista`
Aspect: 16:9 · 3840×2160 · 24 fps · 276.00 s · aac stereo
Run: `ca12337c8e2c43d0a0add834b390efd7` (source SHA updated; renders verified unchanged from prior clean build)

## Beat classification (PROOF GATE from `nopunt` SKILL.md)

12 SHOW / 0 justified-HOLD / 0 PUNT-flagged.
Every body beat classifies SHOW: it names its on-screen artifact in `shot.show`
and the visual carries evidence the voice reacts to. The bookend beats (B00
ASK, B11 OUTRO) render UI directly. No unresolved slates.

## Teaching arc checklist (nopunt § whole-sheet)

- FRAMEWORK — ✓ (B02 anatomy + B03 pipeline, before any mechanism act)
- WORKED EXAMPLE — ✓ (B04 GATE ASK on a concrete default ask; B06 shows two
  example calls in narration + on-screen body)
- FALSIFIABILITY — ✓ (B07 explicitly separates GENERATOR vs DESCRIBER error;
  narration says the case-not-separable line too)
- SCAFFOLDED TASK — ✓ (B10 Your Turn — Liam reads the prompt aloud and
  discusses the 3-check rubric before inviting the pause)
- BOOKENDS — ✓ (B00 cold open · B01 hesitant-writer BLUF · B09 verdict card ·
  B10 handoff · B11 title-restate outro)
- NO-SOURCE-NO-VERDICT — ✓ (every substantive claim in narration is traced to
  a SKILL.md line in FACTCHECK.md; no fabricated fashion demo)

## Motion histogram

`reveal:8  type-on:3  hold:1` — compile.py warns that `reveal` carries 66% of
beats vs. the ~40% pantry cap. Accepted here: the skill-teardown modifier is
structurally reveal-heavy (each mechanism beat is a reveal-in of eyebrow →
heading → body → quote → verdict pill), and the two type-on beats (B00, B10)
plus the hesitant-writer type-on and the hold-outro do the pacing work. This
warning is not a Gate refusal — the final master compiled cleanly.

## Kokoro audio (master clock)

All 12 beats generated by `runtime/scripts/generate_audio_kokoro.py` using
voice `am_onyx` (free, local). Actual durations (measured from mp3 decode):

| Beat | Actual (s) | Kind |
|---|---|---|
| B00 | 15.00 | ASK |
| B01 | 15.23 | BLUF |
| B02 | 27.01 | ANATOMY |
| B03 | 25.47 | PIPELINE |
| B04 | 21.06 | MECH · GATE ASK |
| B05 | 24.75 | MECH · MASTER CLOCK |
| B06 | 26.90 | MECH · CONFIDENCE |
| B07 | 25.45 | FALSIFIABILITY |
| B08 | 28.52 | DESIGN TELL · CALLS |
| B09 | 28.65 | VERDICT |
| B10 | 34.05 | YOUR TURN |
| B11 | 3.67  | OUTRO |
| **Total** | **275.76** | |

## Compile

Command:
```
python3 runtime/scripts/compile.py <reel> --height 2160 --out <reel>/exports/landscape
```

Result:
```
[art] compiled B00  VIDEO    15.0s  ← B00.mp4
… (12 beats compiled, all VIDEO)
[art] wrote …/claude-liam-brutalist-skill-fashionista.mp4  (276.0s, audio: per-beat timeline …)
[art] slots: 12/12 filled — B00:VIDEO B01:VIDEO … B11:VIDEO
```

ffprobe on the final MP4:
```
$ ffprobe -v error -select_streams v:0 -show_entries stream=width,height,r_frame_rate,duration -of csv=p=0
3840,2160,24/1,276.000000
$ ffprobe -v error -select_streams a:0 -show_entries stream=codec_name,channels -of csv=p=0
aac,2
```

## Gate V (frame-level visual QC)

Frame-level visual QC ran as part of `compile.py` and passed cleanly (no
BLOCKER, no MAJOR defects). The compiler's atomic verification would have
refused the master otherwise; the `.verified.json` sidecar exists.

## Visual review (LLM-eye pass)

Extracted per-beat frames at 15%, 50%, 85% for every beat and inspected them
with the image-capable Read tool. Contact sheet at
`_qc/contact-landscape.png`. Findings:

- B00 — composer displays "Hallo, Liam" greeting, ask lands answered with
  three result lines, terracotta send button armed, `@HumanitariansAI` folder
  chip visible. No overflow.
- B01 — hesitant writer completes the sentence "Fashionista is a fashion
  experiment. Every episode is one trial. You are the judge." The correction
  from `tutorial → experiment` is visible in the timeline.
- B02 — anatomy card is legible; the callout box states "The artifact is the
  CALLS.json row."; spark line reads "The gate is the experiment."
- B03 — horizontal pipeline shows INTRO → THE VIDEO (terracotta) → OUTRO,
  bracketed by INPUT and OUTPUT nodes. Footer note lands: "Total runtime =
  clip + ~5 s. Video is the master clock…"
- B04-B08 — mechanism cards each land eyebrow, heading, body, verbatim quote
  from SKILL.md (with source citation), and a verdict pill. Everything inside
  the safe area.
- B09 — verdict card shows six numbered lines, terracotta numerals, heading
  "An experiment. Not a series."
- B10 — Your Turn composer types the paste-ready prompt with the three-check
  output rubric.
- B11 — outro shows the title "The fashionista Skill." with terracotta
  period, hardcoded @NikBearBrown handle (OUTRO-LOCK.md), slug-seeded mascot.

No overlap, no clipping past safe, no unwanted captions/subtitles, no
unresolved slates. Every beat teaches the concept the narration describes.

## Availability log (gaps recorded honestly)

- The legacy `scripts/type_check.py` referenced by ai-explainer's "GATE T ·
  ALWAYS RUN" is NOT present in this toolkit. TYPECHECK.md documents the gap
  and the substituted visual typography review.
- The `qc/final_frame_check.py` gate (Gate V) IS present and ran successfully.
- The `scenes/ILLUSTRATIONS.md` referenced by ai-explainer is absent; the
  isolated toolkit has a working `Illu-*` search hit via
  `./art scenes`, and beats were composed against that library only.

## Conclusion

Landscape master: **CLEAN**. All checks that exist in this toolkit passed.
Bear's viewing/editing is next; the file is not published.
