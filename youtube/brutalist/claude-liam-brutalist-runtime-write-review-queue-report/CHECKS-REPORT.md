# CHECKS REPORT — Brutalist Utility: write_review_queue_report.py

Run ID: `aef36acf6a354924acd1af29d9d319cf`  
Date: 2026-09-14  
Source SHA-256: `8de57eb1bb0e4f1345c7a7e26ad99365969eda321c3a7fc81d897867e50d904d`

---

## Gate A — Static Preflight

All 12 beats carry `shot.remotion.pattern` or equivalent. No Manim scenes in this reel. Preflight clean.

---

## Gate B — Render

All 12 Remotion beats rendered to `media/` via `remotion_scenes.py`. Scale `--scale=2` used for 4K supersampling. `--image-format=png` used to avoid JPEG quality ceiling. `--crf=16` for clean encode.

Beat durations match `actual_duration_s` fields in beat sheet. `extend_clip_to_duration` applied for duration conformance.

**Landscape renders:** B00–B11 all `ok` — 12/12.  
**Vertical renders:** B00–B11 all `ok` — 12/12.  
Portrait-native 916 variants used throughout. B02 uses ClaudeCodeBeat916 (SkillTeardownPipeline bleeds in portrait). B06/B08 use SkillTeardownMechanism916.

---

## Gate V — Visual QC (final_frame_check.py)

**Landscape master:** `final_frame_check.py` run inside `compile.py` on the candidate MP4.  
Result: `frames=24  BLOCKER=0  MAJOR=0` — **PASS**

**Vertical master (first attempt):** 4 BLOCKERs — B01 left/right bleed, B10 right bleed.

### B01 Edge-Bleed Fix

Root cause: `BrutalistHesitantWriter916` with text `"write_review_queue_report.py\ngenerates the audit.\nIt inspects every reel."` at `fontSize=150`. The 28-char filename line at `size=300px` (scale=2) extends ~4600px from center — massively past `safe_r=2052`. Also: `whiteSpace:'pre'` prevents CSS word-wrap; only newlines break lines.

Fix: Split text into 6 shorter lines (`write_review\nqueue_report.py\ngenerates\nthe audit.\nIt inspects\nevery reel.`). Longest line: `queue_report.py` = 15 chars. Reduced `fontSize` from 150 → 120. Measured ink width at `size=240px`: ~1486px half-width from center = right edge 1080+743=1823px < `safe_r=2052` (229px margin). Coverage: 60.5% at 50%, 74.3% at 85% — both above FILL_MIN=55%.

### B10 Edge-Bleed Fix

Root cause: `ClaudeComposerAsk` segment div (`position:absolute, left:PAD_X`) had no `right` constraint. At portrait 4K (height=3840), `fontSize=height*0.038=145.9px`. The 32-char segment "Run it on your own audit output" extends from `left=173px` → `right≈2739px` > `safe_r=2052`. Measured bleed: 44px past `safe_r`.

Fix: Added `right: PAD_X` and `wordBreak:'break-word' as const` to both the segment and topic divs in `ClaudeComposerAsk.tsx`. The segment now wraps to 2 lines in portrait. No impact on landscape (text fits on one line in 3840px-wide canvas).

**Vertical master (after fixes):** `frames=24  BLOCKER=0  MAJOR=0` — **PASS**

---

## Audio Check

All 12 beats have `audio_file: mp3/beat-B*.mp3` entries. Audio files generated by Kokoro TTS (`am_onyx` voice). Durations validated during compile: each beat's audio duration matches `actual_duration_s`.

Both masters include audio: `akind=per-beat timeline (source audio preserved)`.

---

## Compile Output

| Aspect     | File                                                           | Duration | SHA-256 (first 16)    |
|------------|----------------------------------------------------------------|----------|-----------------------|
| Landscape  | `exports/landscape/claude-liam-...mp4`                         | 222.5s   | `e4946432a7e44d63...` |
| Vertical   | `exports/vertical/claude-liam-...-vertical.mp4`                | 222.5s   | `e4fd52e7ca0de75f...` |

Full SHA-256 hashes in `VISUAL-REVIEW.json`.

---

## Slates

0 slates in final masters. All 12 slots filled in both aspects.

---

## TypeScript Typecheck

See `TYPECHECK.md`.

---

## Known Gaps

- Landscape B01 uses original text with long filename line, which is fine at landscape scale (3840px wide canvas). Portrait B01 uses split 6-line text.
- ClaudeComposerAsk.tsx change (right:PAD_X on segment/topic) affects all future renders of that component. Existing landscape renders not affected (text fits without right constraint).
- `SkillTeardownMechanism916` was added to Root.tsx as a new composition alias during this build.

---

Bear review required before publication.
