# FACTCHECK — The screen-clean Skill

Every factual claim in the film is traced to `screen-clean/SKILL.md` or to
a real command captured in `demo/screen-clean-run.log`. No claim is invented,
no result is attributed to an old fellow's reel.

## B00 — cold open (composer)

- "shared preprocessor" · "Zoom, Teams, Meet, QuickTime" — SKILL.md
  frontmatter: "Prepare ANY screen recording (Zoom, Teams, Meet, QuickTime)
  for use as a beat inside a reel."
- "Five passes" — SKILL.md frontmatter and the five `## Pass N` headings.
- "One review artifact" — SKILL.md § Output contract: "the contact-sheet
  path" as one of five returned items. § Pass 2 also: "the proof is the
  review artifact, not the numbers".
- "Never publishes" — SKILL.md frontmatter final sentence.

## B01 — BLUF

- "The name reads like a cleaner. It is not one." — pedagogy of the hesitant
  writer, framed on the doctrine. SKILL.md never uses the word "cleans" in a
  sanitizing sense.
- "does not sanitize your desktop or decide what is embarrassing on your
  behalf" — direct paraphrase of § Pass 4: "'is this embarrassing' is
  judgment, not arithmetic".
- "safe defaults" — SKILL.md § Pass 2: "**B is the default.**" and
  "Under `--silent`, apply B, write the contact sheet to the review queue".
- "flags what a human still needs to look at" — § Pass 4: "wait for a human".
- "The proof of a good pass is … the contact sheet at the end" — § Pass 2:
  "**The proof is the review artifact, not the numbers**".

## B02 — anatomy

- "one file — skills/make/screen-clean/SKILL.md" — verified on disk in the
  isolated toolkit. The `screen-clean/` folder contains only `SKILL.md`.
- "Fellows calls it. Guests calls it." — SKILL.md § Trigger: "Called by
  `fellows`, `guests`, or any skill placing a recording into a beat."
- "the state fellows was in before this skill existed" — SKILL.md § "Why it
  is shared, not built into one skill": "which is the state `fellows` was in
  before this skill existed."

## B03 — pipeline

- Five passes and their names — SKILL.md § headings verbatim: "Pass 1 — probe
  and audio gate", "Pass 2 — aspect fit", "Pass 3 — legibility", "Pass 4 —
  privacy scan (flag, never decide)", "Pass 5 — dead-air trim".
- "Middle edits are the caller's job, not this one's" — SKILL.md § Pass 5:
  "Never trim the middle — that is an edit of someone's talk, and it belongs
  to the calling skill's human gate, not here."
- Output nodes ("cleaned 1920×1080 beat + contact sheet") — § Output
  contract: five items including "the cleaned video at 1920x1080" and "the
  contact-sheet path".

## B04 — mechanism 1 (ignorant of who is speaking)

- "deliberately ignorant of who is speaking" — SKILL.md § intro:
  "deliberately **ignorant of who is speaking**".
- "The relation belongs to the caller" — SKILL.md § intro: "the editorial
  relationship (mentee vs board member vs guest) belongs to the calling
  skill, never here."
- "Format is orthogonal to relationship" — SKILL.md § Why it is shared:
  verbatim quote.
- "one of them ships letterboxed frames — the state fellows was in before
  this skill existed" — same paragraph, verbatim.

## B05 — mechanism 2 (audio gate)

- "decodes the actual audio — with ffmpeg volumedetect" — SKILL.md § Pass 1:
  "the **decoded** `mean_volume` from `ffmpeg -af volumedetect`".
- "-40 dB stop" — SKILL.md § Pass 1: "**If mean_volume is below -40 dB,
  STOP.**"
- "sample a ninety-second window from the middle" — SKILL.md § Pass 1: "sample
  a 90-second window from the middle rather than decoding the whole thing".
- Numeric measurements (`-91.0 dB` silent, `-21.1 dB` voiced) — captured in
  `demo/screen-clean-run.log` on fixtures generated by `ffmpeg -f lavfi` in
  the same session. Reproducible; commands preserved.

## B06 — mechanism 3 (aspect fit)

- "sixteen by ten" source vs "sixteen by nine" reel — SKILL.md § Pass 2:
  "A screen recording is not a reel beat. It is 16:10 when the reel is 16:9".
- Route A "1728×1080, 96px bars each side" — SKILL.md § Pass 2 table.
- Route B (default) "1809×1080, 56px bars" · "~72 px taskbar" — same table
  and paragraph, verbatim.
- Route C "1920×1080, no bars, ~88 px shared content removed" — same table
  and the explanation immediately after ("reaching a true 16:9 needs 160
  rows gone but the taskbar is only ~72 — the other 88 come out of the
  shared screen itself").
- "Bars are Claude cream `#FAF9F5`, never black" — SKILL.md § Pass 2,
  verbatim.
- "never crop from the top" — SKILL.md § Pass 2: "**Never crop from the
  top.** Webcam thumbnails sit at the top-right, starting at y=0."

## B07 — mechanism 4 (privacy)

- Full list ("clock … browser tab titles … notification toasts … filenames
  … calendar or mail content") — SKILL.md § Pass 4 verbatim bullet list.
- "marks which items survive the crop and which the crop removes" — SKILL.md
  § Pass 4: "mark which items **survive the crop** and which the crop
  removes".
- "does not auto-blur, does not decide" — § Pass 4: "Never auto-blur and
  never decide on someone's behalf".
- "matters more for fellows … than for board members" — SKILL.md § Pass 4:
  "This matters more for fellows than for guests: board members present
  deliberately, fellows are often on personal machines with their own life
  on screen."

## B08 — falsifiability (contact sheet)

- "seventy-two pixels cropped and fifty-six pixel bars is not something a
  human can judge in the abstract" — SKILL.md § Pass 2: "'Cropped 72px,
  bars 56px' is not something a human can judge."
- "ten, thirty-five, sixty and eighty-five percent" — SKILL.md § Pass 2:
  "four frames sampled across the recording (~10%, 35%, 60%, 85%)".
- "Four frames, not one — the shared screen changes." — SKILL.md § Pass 2
  verbatim.
- "A crop that costs nothing while the speaker is on a web page may clip a
  terminal six minutes later" — SKILL.md § Pass 2 verbatim.

## B09 — verdict

- All six lines are compressed restatements of the six mechanism lines above,
  each independently sourced to the SKILL.md in this document. No new claims.

## B10 — Your Turn

- Instructs the viewer to run `ffprobe` and `ffmpeg -af volumedetect` on a
  synthetic fixture, then reason about the A/B/C table without cropping a
  real file. Every referenced tool is one the skill's own doctrine invokes
  (§ Pass 1 volumedetect; § Pass 2 aspect measurement). The prompt is safe
  by construction — nothing personal is touched.

## B11 — outro

- "Liam, in for Bear" — IN-FOR-BEAR LAW (parent `ai-explainer/SKILL.md`
  § Channels).
- Handle `@NikBearBrown` — OUTRO-LOCK for `claude-liam` reels.

## Deliberate omissions

- Higgsfield / paid tiers — not relevant to this skill; not mentioned to keep
  the film honest about the free path.
- ElevenLabs — the parent doctrine notes it was removed 2026-09-03; the
  screen-clean film has no audio-generation content to warrant repeating it.

## Boundary — what the film does NOT claim to have executed

- No real screen recording was ingested. Passes 2 through 5 are illustrated
  from the SKILL.md, per REBUILD LAW, and labelled "Redrawn from SKILL.md" on
  screen where a table or list is shown.
- The `--silent` mode, the actual contact-sheet renderer, and the dead-air
  trim implementation are described from the SKILL.md; the film does not
  claim to have exercised them end to end.
